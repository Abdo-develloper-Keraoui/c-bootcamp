#include <stdio.h> 
int main(void) 
{
	//left-justified vs right-justified (%-m.pX) this is the formula
//	printf("--[%10d]--\n", 2002); // 10 digits min right justifed, meaning that it gets pushed to the right and we don't use the minus, so right ==> plus sign
//	printf("--[%-10d]--\n", 2002);// 10 digits min, left justified, meaning that it gets pushed to the left and we use the minus, left ==> minus  

	printf("--\t--\n");
	return 0;
	
}
